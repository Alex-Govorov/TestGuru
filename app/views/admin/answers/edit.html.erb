<h1><%= t('.header') %></h1>

<p>
	<%= @answer.question.body %>
</p>

<%= render 'form' %>

<%= link_to t(:show), admin_answer_path(@answer), class: 'btn btn-primary' %>
<%= link_to t(:back), admin_question_path(@answer.question), class: 'btn btn-secondary' %>
