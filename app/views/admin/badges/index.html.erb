  <h1 class="mt-2"><%= t('.header') %></h1>
  <%= render 'shared/errors', resource: @badge %>
  <div class="row justify-content-left mt-2">

    <table class="table">
  <thead>
   <tr>
    <th scope="col" class="text-center"><%= t('.image') %></th>
    <th scope="col" class="text-center"><%= t('.title') %></th>
    <th scope="col" class="text-center"><%= t('.rule') %></th>
    <th class="text-center"><%= t('.actions') %></th>
  </tr>
</thead>
<tbody>
  <%= render @badges %>
</tbody>
</table>

      <%= link_to t('.new_badge'), new_admin_badge_path, class: 'btn btn-primary' %>
  </div>
