<h1><%= @question.body %></h1>

<h2><%= t('.header') %></h2>



<ol>
	<%= render @question.answers %>
</ol>
<%= link_to t('.new_answer'), new_admin_question_answer_path(@question), class: 'btn btn-primary' %>
<%= link_to t(:back), admin_test_path(@question.test), class: 'btn btn-secondary' %>
