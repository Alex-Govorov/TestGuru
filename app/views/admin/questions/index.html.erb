<% if @questions.exists? %>
	<h2>Questions</h2>

	<table>
		<tr>
			<th>Body</th>
			<th colspan="3">Actions</th>
		</tr>

		<%= render @questions %>
	</table>
<% end %>
<%= link_to 'New question', new_admin_test_question_path(@test) %>
