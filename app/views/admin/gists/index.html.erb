<h1>Gists</h1>

<table class="table">
  <thead>
   <tr>
    <th scope="col"><%= Question.model_name.human %></th>
    <th scope="col">Gist URL</th>
    <th scope="col"><%= User.human_attribute_name(:email) %></th>
  </tr>
</thead>
<tbody>
  <%= render @gists %>
</tbody>
</table>

<%= link_to t(:back), admin_tests_path, class: 'btn btn-secondary' %>
